name: Publicar en Firebase (legacy — desactivado)

# Firebase Cloud Functions requiere plan Blaze (facturación).
# La wallet migró a Supabase. Usa deploy-supabase-wallet.yml

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "::warning::Este workflow está obsoleto."
          echo "Usa «Deploy wallet Supabase» en su lugar."
          echo "Firebase Spark (gratis) no permite Cloud Functions."

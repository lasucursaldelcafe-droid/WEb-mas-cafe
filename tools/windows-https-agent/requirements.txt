# Agente HTTPS mascafé.com — dependencias mínimas (stdlib + requests)
requests>=2.31.0,<3
python-dotenv>=1.0.0,<2

# Opcional — bandeja del sistema Windows (python agent.py tray)
# pystray>=0.19.5
# Pillow>=10.0.0

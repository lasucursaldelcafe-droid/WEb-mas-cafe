name: Setup autónomo — Wallet Supabase (legacy Firebase desactivado)

# Reemplazado por deploy-supabase-wallet.yml

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::warning::Wallet migrada a Supabase (plan gratuito)."
          echo "Ejecuta el workflow «Deploy wallet Supabase»."
